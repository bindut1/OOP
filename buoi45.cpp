#include <iostream>
#include "Point.h"
/* #include "Point1.h"
#include "Point2.h" */
//#include "TG.h"
using namespace std;

//chi phan van khi bien truyen vao la tham chieu hoac bien thuong, con bien con tro thi tuc nhien la khac xa va se nap chong binh thuong
// void a(){cout<< 1;}
// void a(int x=4){cout<< x;} 

//ham thanh vien tinh thong thuong chi tuong tac voi bien hang tinh hoac bien tinh


main()
{
   /*  Point2 p1;
    Point2 p2(2, 3);
    Point2 p3(p1);
    // int x=9;
    // a(5);

    p1.Show();
    p2.Show();
    p3.Show(); */

    // Point p1(3, 4);
    // //changePoint(p1);
    // p1.Show();

    /* cout << Point::n << endl;
    Point p1(3, 4, 6);
    Point *p = new Point(1, 2, 3);
    cout << Point::n << ", " << p1.n << ", " << p->n << endl;
    delete p;
    cout << Point::n << ", " << p1.n << endl; */
    /* Point::Print();
    Point p1(3, 4, 6);
    Point *p = new Point(1, 2, 3);
    Point::Print();
    p1.Print();
    p->Print();
    delete p;
    Point::Print();
    p1.Print(); */

    /* Point p1(1, 2), p2(2, 3), p3(3, 4);
    TG t1(p1, p2, p3);
    TG t2(9, 8, 7, 6, 5, 4);
    t1.ShowTG();
    t2.ShowTG(); */

    /* int *p = new int[3];
    Point *p1 = new Point[3];
    for(int i = 0; i < 3; i++ )
        (p1 + i)->Show(); */


        /* Point p1(1, 2), p2(2, 3);
        Point p3 = p1 + p2;
        Point p4 = operator+(p1, p2);
        p3.Show();
        p4.Show();

        Point p5 = p2 - p1;
        Point p6 = p2.operator-(p1);
        Point p7 = p1.operator-(p2);
        Point p8 = p1 - p2;

        p5.Show();
        p6.Show();
        p7.Show();
        p8.Show();  */
    /* Point p1(1, 2), p2(2, 3);
    cin >> p1;
    cout << p1;
    p1 << cout;
    cout << p1 << p2; */
//    p1 << p2 << cout; //bao loi khi xuat ra 2 gia tri tro len
}

//khi da nang hoan toan tu cin cout thi nen khai bao la mot ham toan cuc, neu khai bao la mot ham thanh vien cua lop thi chi dung trong mot so truong hop