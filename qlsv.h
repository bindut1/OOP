#include "sv.h"
#include <iostream>
using namespace std;
/* 
class qlsv
{
    private:
        sv *p;
        int n;
    public:
        qlsv();
        ~qlsv();
        friend ostream& operator<<(ostream&, const qlsv&);
        sv& operator[](const int&);
        void add(const sv&);
        void insert(const sv&, const int&);
        //tim trong mang co phan tu SV nao trung ko
        //neu trung tra ve vi tri phan tu trung
        //neu ko co tra ve -1
        int indexOf(const sv&);
        void remote(const sv&);
        void remoteAt(const int&);
        void update(const int&);
}; */