#include <iostream>
#include "qlsv.h"
using namespace std;

main()
{
    /* qlsv db;
    sv A(1, true, 3.4);
    sv B(2, false, 3.9);
    sv C(3, true, 4.0);
    sv D(4, true, 2.8);
    db.add(A);
    db.add(B);
    db.add(C);
    cout << db.indexOf(C) << endl; */
}